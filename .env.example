# Cloudflare API credentials for admin provisioning
# Copy to .env and fill in. NEVER commit .env to git.
CF_API_TOKEN=your_api_token_here
CF_ACCOUNT_ID=your_account_id_here

# Optional: custom Zero Trust auth domain
# AUTH_DOMAIN=ov-websites.cloudflareaccess.com
